{
    "builds": [{
        "src": "weatherapp/wsgi.py",
        "use": "@burundukov/vercel-python-wsgi"
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "weatherapp/wsgi.py"
        }
    ]
}